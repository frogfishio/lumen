struct Pair[A, B] {
    first: A,
    second: B,
}

fn swap[A, B](p: Pair[A, B]) -> Pair[B, A] {
    Pair { first: p.second, second: p.first }
}

pub fn main() -> i32 {
    let p = Pair { first: 1, second: 2 };
    let swapped = swap(p);
    swapped.first
}
