fn describe(opt: Option[Int]) -> String {
  match opt {
    Some(x) if x > 0 => "positive {x}"
    Some(0)          => "zero"
    Some(x)          => "negative {x}"
    None             => "missing"
  }
}
