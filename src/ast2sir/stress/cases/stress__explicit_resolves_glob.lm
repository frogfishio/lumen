mod util { pub fn add() -> I32 { 1 } }
mod math { pub fn add() -> I32 { 2 } }

use util::*;
use math::*;
use util::add; // Explicit should resolve ambiguity

pub fn main() -> i32 {
    add() // Should work: explicit wins
}