pub fn main() -> i32 {
    let x = 10;
    if true {
        let x = 20; // Shadows in if block
        x
    } else {
        let x = 30; // Shadows in else block
        x
    } + x // Outer x still accessible
}