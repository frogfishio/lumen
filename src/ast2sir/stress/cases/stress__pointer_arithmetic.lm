fn main() {
    let arr = [1, 2, 3, 4, 5];
    unsafe {
        let ptr = &arr[0] as *const I32;
        let next = ptr + 1;
        let val = *next;
        let prev = next - 1;
        let val2 = *prev;
        let _ = val + val2;
    }
}