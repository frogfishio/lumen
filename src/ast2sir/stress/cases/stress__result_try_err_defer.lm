// Stress: Result[T,E] + try + Err path + defer interaction.

fn ok(x: I32) -> Result[I32, I32] {
  if x == 0 as I32 {
    Err(7 as I32)
  } else {
    Ok(x)
  }
}

fn f() -> Result[I32, I32] {
  let mut acc: I32 = 0 as I32;
  defer { acc = acc + (1 as I32); }

  let a: I32 = try ok(1 as I32);
  let b: I32 = try ok(0 as I32);

  Ok(a + b + acc)
}

fn main() {
  let _ = f();
}
